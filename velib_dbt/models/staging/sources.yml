version: 2

sources:
  - name: raw
    database: VELIB_RAW
    schema: RAW
    tables:
      - name: raw_availability
        description: "JSON bruts disponibilité Vélib depuis GCS (1 row = 1 fichier)"
      - name: raw_stations
        description: "JSON bruts stations Vélib depuis GCS (1 row = 1 fichier)"
